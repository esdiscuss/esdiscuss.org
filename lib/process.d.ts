export declare function processMessage(body: string): string;
export declare function renderMessage(body: string): string;
export declare function processNote(content: string, date: string, isNotMeetingNotes: boolean, month: string): string;
